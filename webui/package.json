{
  "name": "deep-context-agent-webui",
  "private": true,
  "version": "0.18.0",
  "scripts": {
    "build": "esbuild src/app.ts --bundle --format=iife --minify --target=es2020 --outfile=../src/context_agent/static/app.js",
    "check": "tsc --noEmit && node tests/bundle.test.mjs"
  },
  "devDependencies": {
    "esbuild": "^0.25.0",
    "typescript": "^5.9.0"
  }
}
